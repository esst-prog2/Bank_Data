# Planning log

One line per decision: date, what was decided, who decided it (user or agent).
Append only — never rewrite an earlier line. See `AGENTS.md` for the rule.

- 2026-09-22: Adopted a planning log (this file) plus `AGENTS.md` as the project's
  portable agent-memory file, replacing the standalone `CLAUDE.md` (now a one-line
  pointer, `@AGENTS.md`). Decided by user.
- 2026-09-22: Adopting OpenSpec (Fission-AI/OpenSpec) for spec-driven development —
  `openspec/specs/` as source of truth for current behavior, `openspec/changes/` for
  proposals. Decided by user. The `openspec` CLI itself could not be installed or run
  from this session (no npm registry access in the sandbox, and no shell bridge to the
  user's machine in this session), so the `openspec/` scaffold below was hand-written
  to match the CLI's documented conventions rather than generated by `openspec init`.
  The user should run `npm install -g @fission-ai/openspec@latest` then
  `openspec init --tools claude` locally to get the real CLI, its skill/command files,
  and `openspec doctor`/`openspec validate` to check this scaffold. Decided by agent
  (environment constraint), flagged to user.
- 2026-09-22: First MVP scope drafted as an OpenSpec change proposal
  (`openspec/changes/add-mvp-v1/`), taken directly from README.md section 3 ("The
  size"): a defined set of financial-statement and regulatory data for supported
  banks, returned as a pandas DataFrame, with source/comparability metadata and
  explicit missing-vs-failed distinction; explicitly excludes full EU coverage, a
  dashboard/API, and analysis/modelling. Decided by user (via README), captured by
  agent.
- 2026-09-22: Installed Node.js (LTS, v24.19.0) and the real OpenSpec CLI
  (`@fission-ai/openspec@1.13.1`) on the user's machine, then ran
  `openspec init --tools claude` in this repo as the previous entry asked. It
  recognized the hand-written `openspec/config.yaml` and left it untouched (only
  added an empty `openspec/changes/archive/` folder plus the real `.claude/skills/`
  and `.claude/commands/opsx/` Claude Code integration), and `openspec validate
  --all` passed the hand-written `add-mvp-v1` change with no errors. Added a
  `.gitignore` (data/raw/, data/state/, data/entities.csv, data/modules.txt,
  __pycache__/, .venv/) since none existed yet, matching the not-committed-empty
  convention `AGENTS.md` already describes for `data/entities.csv`. Pushed the
  whole repo to `github.com/esst-prog2/Bank_Data` (this project's actual submission
  repo, separate from `Advanced_Programming_Marton_Nagy`) for the first time — prior
  work here had never been committed. Decided by user, carried out by agent.
